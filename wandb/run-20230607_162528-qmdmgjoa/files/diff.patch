diff --git a/main.py b/main.py
index 239e013..83678cc 100644
--- a/main.py
+++ b/main.py
@@ -6,15 +6,15 @@ from utils import seed_everything
 # Training settings
 hparams = {
     "batch_size": 128,
-    "epochs": 20,
+    "epochs": 5,
     "lr": 3e-5,
     "gamma": 0.7,
     "image_size": 256,
-    "patch_size": 32,
-    "dim": 1024,
-    "depth": 24,
-    "heads": 16,
-    "mlp_dim": 4096,
+    "patch_size": 16,
+    "dim": 64,
+    "depth": 2,
+    "heads": 8,
+    "mlp_dim": 128,
     "frame_patch_size": 2,
     "temporal_depth": 6
 }
diff --git a/train.py b/train.py
index 99a6150..92b2ff5 100644
--- a/train.py
+++ b/train.py
@@ -115,8 +115,8 @@ def train_vit(df, split_type, images_path, hparams):
         train_data = MAPFDataset(images_path, train_df, transform=train_transforms)
         val_data = MAPFDataset(images_path, test_df, transform=validation_transforms)
 
-        train_loader = DataLoader(dataset = train_data, batch_size=hparams["batch_size"], shuffle=True)
-        valid_loader = DataLoader(dataset = val_data, batch_size=hparams["batch_size"], shuffle=True)
+        train_loader = DataLoader(dataset = train_data, batch_size=hparams["batch_size"], shuffle=True, num_workers=4)
+        valid_loader = DataLoader(dataset = val_data, batch_size=hparams["batch_size"], shuffle=True, num_workers=4)
 
         model = SimpleViT(
             image_size = hparams["image_size"],
@@ -284,4 +284,4 @@ def validation_step(model, criterion, valid_loader, device):
         "eval/coverage": epoch_val_coverage,
         "eval/coverage runtime": epoch_val_coverage_runtime,
         "eval/loss": epoch_val_loss
-    }
\ No newline at end of file
+    }
